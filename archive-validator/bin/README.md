Place trusted release-built `synergy-archive` and `synergy-node` binaries here before packaging, or pass their paths to the macOS extracted-zip installer.

This package intentionally does not include private keys, chain data, or local secrets.
